# Workspace Rules

## Automatic Commit and Push
- Whenever a bug is fixed or a debugging issue is resolved, verify all tests pass, stage all modified files, create a concise git commit, and automatically `git push` to GitHub without waiting for a separate request.
